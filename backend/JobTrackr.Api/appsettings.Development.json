{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Jwt": {
    "_comment": "Local-development signing key only. This file is loaded only when ASPNETCORE_ENVIRONMENT=Development (the default for `dotnet run`). It is NOT a production secret.",
    "Key": "local-dev-only-signing-key-not-for-production-32+chars"
  }
}
