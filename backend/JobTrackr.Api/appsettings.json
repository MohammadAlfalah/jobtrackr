{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DefaultConnection": "Data Source=jobtrackr.db"
  },
  "Jwt": {
    "_comment": "Key is intentionally empty here. Provide it at runtime via the Jwt__Key environment variable (see docker-compose.yml) or user-secrets. For local dev it is set in appsettings.Development.json. The app refuses to start without a key of at least 32 bytes.",
    "Key": "",
    "Issuer": "JobTrackr",
    "Audience": "JobTrackrClient"
  },
  "Cors": {
    "FrontendOrigin": "http://localhost:5173"
  }
}
